<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        fieldset{
            width:350px;
            margin:10px 0px;
        }
    </style>
</head>
<body>
    <h1> <%= title %></h1>
    <form action="/training2Result" method="POST">
        <div>
            이름 : <input type="text" placeholder="이름" name="name" required/>
        </div>
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자" id="남자" required> 남자
            <input type="radio" name="gender" value="여자" id="여자" required> 여자
        </fieldset>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year">
                <% for (var i=1974; i<2023; i++ ){ %>
                    <option value=<%= i %>`><%= i %></option>
                <% } %>
            </select>
            <label for="year">년</label>
            <select name="month">
                <% for (var i=1; i<=12; i++ ){ %>
                    <option value=<%= i %>><%= i %></option>
                <% } %>
            </select>
            <label for="month">월</label>
            <select name="day">
                <% for (var i=1; i<=31; i++ ){ %>
                    <option value=<%= i %>><%= i %></option>
                <% } %>
            </select>
            <label for="day">일</label>
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="hobby" value="여행"> 여행
            <input type="checkbox" name="hobby" value="패션"> 패션
            <input type="checkbox" name="hobby" value="음식"> 음식
        </fieldset>
        <fieldset>
            <legend>선택</legend>
            <span> 좋아하는 색상 <input type="color" name="color"></span>
            <br/>
            <span> 좋아하는 숫자 <input type="number" name="number"></span>
        </fieldset>
        <button type="submit">회원가입</button>
    </form>
</body>
</html>